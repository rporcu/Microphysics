# Run time pf simulation
amr.stop_time = 10.00
##mfix.fixed_dt = 1

# Number of grid cells in each direction at the coarsest level
amr.n_cell = 25 4 4 

# Maximum allowable size of each subdomain in the problem domain;
#    this is used to decompose the domain for parallel calculations.
#WRONG amr.max_grid_size = 93 20 12
amr.max_grid_size_x = 1024
amr.max_grid_size_y = 1024
amr.max_grid_size_z = 1024

# Maximum tile size within each grid
fabarray.mfiter_tile_size = 1024 1024 1024

# Maximum particle tile size
particles.tile_size       = 1024 1024 1024

# SIMPLE or Projection Method for p-v coupling
## mfix.use_proj_method = 1
## mfix.fixed_dt = 1

# Maximum level in hierarchy (for now must be 0, i.e., one level in total)
amr.max_level = 0

amr.check_int = 100000

amr.plot_int  = 100000
amr.plot_file = R_AMR_

amr.par_ascii_int  = 40
amr.par_ascii_file = R_DES_DATA_


amr.dual_grid  =  0
#mfix.load_balance_type = "KDTree"
#amr.regrid_int = 100

#mfix.write_user = true

# Geometry
geometry.coord_sys   = 0                       # 0: Cartesian
geometry.is_periodic = 0       0       0       # Is periodic in each direction?
geometry.prob_lo     = 0.      0.      0.      # lo corner of physical domain
geometry.prob_hi     = 0.225   0.0381  0.0381  # hi corner of physical domain

# boundary resolution
mfix.levelset__refinement = 8      # how resolved should the levelset be (used in the simulation)
mfix.levelset__pad        = 16      # how far (in refined units) should the levelset extend

mfix.levelset__eb_refinement = 1   # resolution of the eb used for generating the levelset
mfix.levelset__eb_pad        = 2   # how far the eb extends out of the problem domain


