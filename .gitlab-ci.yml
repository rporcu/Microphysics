# cache:
#   key: "$CI_BUILD_REF_NAME"
#   untracked: true
#   paths:
#     - build/

stages:
  - build
  - buildtest
  - test

buildtest_dem01:
  stage: build
  script:
    - cd tests/DEM01
    - ../../configure_mfix
    - make mfix
  artifacts:
    paths:
    - build

buildtest_dem02:
  stage: buildtest
  script:
    - cd tests/DEM02
    - ../../configure_mfix
    - make mfix
  artifacts:
    paths:
    - build

buildtest_dem03:
  stage: buildtest
  script:
    - cd tests/DEM03
    - ../../configure_mfix
    - make mfix
  artifacts:
    paths:
    - build

buildtest_dem04:
  stage: buildtest
  script:
    - cd tests/DEM04
    - ../../configure_mfix
    - make mfix
  artifacts:
    paths:
    - build

buildtest_dem05:
  stage: buildtest
  script:
    - cd tests/DEM05
    - ../../configure_mfix
    - make mfix
  artifacts:
    paths:
    - build

buildtest_dem06:
  stage: buildtest
  script:
    - cd tests/DEM06
    - ../../configure_mfix
    - make mfix
  artifacts:
    paths:
    - build

test_dem01:
  stage: test
  script:
    - cd tests/DEM01
    - ../../configure_mfix
    - make test

test_dem02:
  stage: test
  script:
    - cd tests/DEM02
    - ../../configure_mfix
    - make test

test_dem03:
  stage: test
  script:
    - cd tests/DEM03
    - ../../configure_mfix
    - make test

test_dem04:
  stage: test
  script:
    - cd tests/DEM04
    - ../../configure_mfix
    - make test

test_dem05:
  stage: test
  script:
    - cd tests/DEM05
    - ../../configure_mfix
    - make test

test_dem06:
  stage: test
  script:
    - cd tests/DEM06
    - ../../configure_mfix
    - make test
